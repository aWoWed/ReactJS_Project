let initialState = {
    friends: [
        {id: 1, message: 'Andrew'},
        {id: 2, message: 'Max'},
        {id: 3, message: 'Ilya'},
    ],
}

const sidebarReducer = (state = initialState, action) => {

    return state
}

export default sidebarReducer